//** No se debe cambiar el ejemplo hasta que lo indique el profesor **/
let  s string;let a int ;
let bb boolean;


function  int RR (int n, boolean _b)
{
	input(kk);
	let log boolean;
	log= n < a < 32100	;	
	if (_b && log)	return s;
	return n + RR (n + 1, bb);	
}

let s_s string;

function print (string s, int RR)	
{
	let s_s boolean;
	
	print"++++Helloooooooooooooooooooooooooooooooooooooooooooooooooooooooooo";
	RR= (8.8);
		print(s)
	;if (bb) return kk;
	print RR;		print (4 + (a + 0987)) +3);
}

kk=332211;

RR = 1;

function  cadena string(string kk)
{
	if ((a != 091) || bb) 
	{kk= prin ('{$}', RR(a, bb));
	let xx string;
	xx= kk;
	if (bb) if (bb) 
		return xx;
	}
	      
		return "";
	xx= s_s;




s = ";
a= RR (5, bb, kk);
prin (kk, 99);
return s;
let a boolean ;

a= 91 < 8 + a;



