//No se debe cambiar el ejemplo hasta que lo indique el profesor */
let  s string;
let  a int;
let  bb boolean;

function int  RR (int n, boolean _b)
{
	let log boolean ;
	log= n < a < 88 ;
	input kk;
	if (_b && log)	return log;
	return RR (n * 1, bb);	
}

let string s_s;


function input (string s, int RR)	
{
	print s;
	print"hola;
	print(RR);	input (a);
	if (bb)
		return s;	
		print 4 + a +( 98765 + 8));
}

function cadena string (bool kk)
{
	if (kk) return "";
	else      
	switch (a)
	{
	   case 000000: inpu ('{9}', RR(9, bb)); break;
	   case 00002: 
	   case 0003: let xx string;xx=s;
		if (kk) 
			   break;	   
	   case 004: return(xx);
	   case 05: print("PdL"); break;
	   case 6: kk= (a < 9) || bb;
	}

	xx=s_s;

	RR = 8;
	
	
	let s_s int;
	print "++++Helloooooooooooooooooooooooooooooooooooooooooooooooooooooooooo";
	a= RR (5, kk, kk);

	kk= inpu (s, a);
	return s_s;
	a= 9 < 8 + a;

































																			#
