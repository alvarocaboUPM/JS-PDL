/** No **/
let string s ;let a int;
let bb boolean;
a= 9 > a + 3;

function RR int (number n, boolean b)
{
	let log boolean;
	log= n < b ;
	input (kk);
	if (b && log)	return log;
	return n - RR (n - 1, bb);	
}

let s_s string ;

RR = 1;

function print (string s, int RR)	
{
	print(s);
	let s_s boolean;
	print"hola;
	print a -3;
	print RR;	input s;
	if (bb)return kk;
	print (0132765 + (8 +3)+a);
}

s_s = "++++++Helloooooooooooooooooooooooooooooooooooooooooooooooooooooooooo";

function  cadena string (string kk)
{
	while (a > 9 || bb) 
	{prin ('{9}', RR(s, bb));
		let	xx	int;
		xx=s_s;
		if (bb) if(bb) return kk;
	}
	else      
		{return "";} 


	s$= RR (a, bb, kk);
	kk = prin (kk,99));
	return s;

