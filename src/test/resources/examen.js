/**/ 
let a int;
let bb boolean;
a= 9 > a % 3; 

function RR int (int n, boolean b)
{
	let log boolean;
	log= n > b ; /*Cambio de signo*/
	input kk; /* Quito paréntesis */
	if (b && log)	return log;
	return n % RR (n % 1, bb);	
}

let s_s string ;

RR = 1;

function printeaEsta (string s, int RR)	/* Cambio de nombre */
{
	print(s);
	let s_s boolean;
	print"hola";
	print a %3;
	print RR;	input s;
	if (bb)return kk;
	print (0132765 % (8 %3)%a);
}

/*s_s = "%%%%%%Helloooooooooooooooooooooooooooooooooooooooooooooooooooooooooo"; */

function  cadena string (string kk)
{
    do{
        print ("{9}"); /* Aqui mal ->  RR(s, bb)*/
		let	xx	int;
		xx=s_s;
		if (bb)  return kk; /* No se puede  if(bb)-> nada */
    }
	while (a > 9 && bb); /*|| -> && */

	s= RR (a, bb, kk); /* quito $ */
	kk = "print (kk)"; /* void prin -> print */
	return s_s;
} /* Faltaba llave */